
<h1>Your Upcoming Assignments are:</h1>
<div class="content">
<% display = []%>
<%if @courses.empty? %>
    <p>Looks like you don't have any assignments. <a href="/courses/new">Add A Course</a> to begin</p><br><br><br><br>
<%else %>
    <% @courses.each do |course| %>
    <%if course.assignments.empty? %>  
      <% display << "none"%>
    <%else %>
      <%list_assignment = course.assignments.split("\r\n")%>
      <%list_assignment.each do |a|%>
      <ul>&#9679 <%=a%> ( <a href="/courses/<%= course.id %>"><%=course.name%></a> )</ul>
      <%end%>     
    <%end%>
    <%end%>
    <%if !display.empty? && display.size == @courses.size%>
      <p>Looks like you dont have any assignments. Add assisgnments to your <a href="/courses">courses</a></p><br><br><br><br></p>
    <%end%>  
<%end%>


  
 </div>  

 